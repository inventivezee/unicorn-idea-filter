# Discovery health check — 2026-07-10 23:53 UTC

## Run health
- Latest run: **17/20 done**, 3 rescoring (tasks 3, 5, 9 — all on rescue
  attempt 5, all holding draft verdicts). Zero stale claims, zero failed.

## Errors (last 8h), grouped
| Group | Count | Last seen | Status |
|---|---|---|---|
| OpenAI 429 quota | 54 | 20:23 | Transient (owner billing outage, topped up) — not a code bug |
| `reading 'name'` amnesia | 43 | 19:35 | Fixed in `d5f5a3f` (self-heal); not recurring |
| OpenRouter 400 provider | 21 | 20:00 | Fixed in `401aa5e` (degrade ladder); not recurring |
| Cross-provider model 404s | 32 | 22:09 | Fixed in `6ac4fc3`+`5035d56` (continuity rule); verified decayed to zero |
| Streaming-required | 16 | 19:58 | Fixed in `b6ad6b5`; not recurring |
| Synthesis over output limit | 9 | 19:54 | Historical; superseded by 32k streamed calls |
| `score 40/40` budget | 4 | 20:05 | Historical (pre-cap-raise); revived tasks completed |
| **`score_sync` "no parseable output"** | **8** | **23:00** | **Open — analysis below** |

## Open item: final score_sync parse failures (tasks 3, 5, 9)
- Shape: the FINAL verdict synthesis (rescoring, dual-panel round 2)
  occasionally produces text with no extractable JSON. 8 occurrences,
  none in the 53 minutes before this check — the affected tasks are
  still progressing (resynth 8–9/40 used).
- Root cause is NOT determinable from the logged error alone (message +
  stack only; the raw model output is not captured). Candidates: model
  prose/refusal on the very large final prompts (first verdict 20k chars
  + verification memo + transcript appendix), or the prompt-embedded
  schema fallback after a grammar-size degrade.
- Why no emergency: the failure is **bounded and self-healing** — retries
  are capped by the resynth budget (40), and on exhaustion the
  draft-promotion path (`5035d56`) publishes the round-1 verdict instead
  of failing the task. Worst case: these 3 tasks publish their drafts.
- Recommended follow-up (needs a human-approved change since it touches
  the synthesis path): capture the first ~2k chars of raw model output in
  the step_error event when parseLastJSON fails, so the next occurrence
  is diagnosable. Optionally: retry-with-smaller-appendix on parse
  failure (drop the transcript appendix on the second attempt).

## Actions taken
- None deployed (per protocol: ambiguous root cause on the publish path).
- No revivals needed: nothing failed from an already-fixed bug.
